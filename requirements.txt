# Core runtime dependencies
pyttsx3>=2.90
SpeechRecognition>=3.10.0

# PyAudio is needed by SpeechRecognition for microphone input.
# On Linux install portaudio first: sudo apt-get install portaudio19-dev
PyAudio>=0.2.13

# Optional: offline speech recognition (alternative to Google Web Speech API)
# pocketsphinx>=5.0.0
